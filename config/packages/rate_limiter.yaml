framework:
    cache:
        pools:
            cache.rate_limiter:
                adapter: cache.adapter.redis
                provider: '%env(REDIS_RATE_LIMITER_DSN)%'

    rate_limiter:
        reservation_create:
            policy: sliding_window
            limit: 5
            interval: '1 minute'
            cache_pool: cache.rate_limiter
